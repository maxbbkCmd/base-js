/**
 * @function calcSum 
 * @param {number} num1
 * @param {number} num2
 * @throws {Error} Если аргумент не число
 * @return (number) сумма чисел
 */

export  const calcSum = (num1, num2) => {
  return num1 + num2;
};

 